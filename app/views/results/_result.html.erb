<%= tag.div id: dom_id(result) do %>
  <div class="flex gap-2">
    <div class="<%= result.success? ? "text-success" : "text-error" %>">
      <%= result.success? ? "Success" : "Fail" %>
    </div>

    <div><%= time_ago_in_words(result.created_at).upcase_first %> ago</div>
  </div>
<% end %>
