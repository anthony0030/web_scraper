<p style="color: green"><%= notice %></p>

<h1>Pages</h1>

<div id="pages" class="flex gap-5 mb-5">
  <% @pages.each do |page| %>
    <%= render page %>
  <% end %>
</div>

<%= link_to "New page", new_page_path, class: "btn btn-success" %>
